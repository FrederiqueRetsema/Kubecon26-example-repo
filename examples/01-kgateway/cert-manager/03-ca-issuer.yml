# ClusterIssuer backed by our self-signed CA
# Can issue certificates in any namespace
# See: https://cert-manager.io/docs/configuration/selfsigned/

apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ca-issuer
spec:
  ca:
    secretName: root-secret
