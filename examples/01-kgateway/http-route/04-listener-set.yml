apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: my-http-listener-set
  namespace: 01-kgateway
spec:
  parentRef:
    name: my-http
    kind: Gateway
    group: gateway.networking.k8s.io
    namespace: kgateway-system
  listeners:
    - name: http-listener-set
      hostname: www.example2.com
      port: 8080
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All
