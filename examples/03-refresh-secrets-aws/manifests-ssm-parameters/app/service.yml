apiVersion: v1
kind: Service
metadata:
  name: gitops-secrets-service-ssm-parameters
spec:
  type: NodePort
  selector:
    app: gitops-secrets-app-ssm-ssm-parameters
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30003
