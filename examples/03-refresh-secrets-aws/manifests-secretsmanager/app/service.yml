apiVersion: v1
kind: Service
metadata:
  name: gitops-secrets-service-secretsmanager
spec:
  type: NodePort
  selector:
    app: gitops-secrets-app-secretsmanager
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30002
