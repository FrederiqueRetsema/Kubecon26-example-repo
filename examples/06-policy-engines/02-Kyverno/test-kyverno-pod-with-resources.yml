apiVersion: v1
kind: Pod
metadata:
  name: test-pod-with-resources-kyverno
  labels:
    team: "conclusion-xforce"
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
